"""Tests for /plan bot command — v2 compatible.

v1 tests (consume, write, plan_start, plan_confirm_impl, plan_confirm_later)
have been replaced by tests/test_plan_integration.py which covers the full
v2 button flow with PlanStatus state machine.
"""
